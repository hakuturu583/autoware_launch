/**:
  ros__parameters:
    update_rate: 10.0
    add_duplicated_node_names_to_msg: true # if true, duplicated node names are added to msg
    nodes_to_ignore:  # List of nodes to ignore when checking for duplicates
      - /rviz2
      - /simulation/getParameter
      - /system/aggregator
      - /system/converter
      - /traffic_light_camera_info_relay
      - /traffic_light_image_relay
      - /trajectory_relay
      - /cam_front_republish
      - /robot_state_publisher
      - /perception/traffic_light_recognition/traffic_light/traffic_light_camera_info_relay
